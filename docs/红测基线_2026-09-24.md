# 红测基线（2026-09-24）

记录改动前已存在的测试失败。此后每阶段合入对比本基线：**只允许「基线内失败」，出现基线外失败 = 回归，必须修复后才能合入。**

## 后端

命令（backend/ 目录下）：

```
./.venv/Scripts/python.exe -m pytest -q -c pytest.ini tests
```

**结果：296 通过 / 0 失败**（本次全绿）。

- `tests/test_auth_isolation.py` 有 2 个偶发 flaky 的历史记录（2026-09-12 基线），本次运行未触发。若后续运行出现该文件内失败，先重跑一次确认是否为已知 flaky，再判断是否回归。
- 无其他已知失败。

## 前端

命令（frontend/ 目录下）：

```
npm test
```

**结果：89 用例中 6 失败 / 83 通过；测试文件 2 失败 / 28 通过。** 与历史基线（report-reader 5 个 + report-presentation 1 个）完全一致。

| 测试 | 状态 | 失败摘要 | 已知 |
|---|---|---|---|
| `src/components/report-reader.test.tsx` > ReportReader > previews a historical version without replacing the current report | FAIL | `useAppStore must be used within AppStoreProvider`（store.tsx:268，组件未包 Provider） | 是 |
| `src/components/report-reader.test.tsx` > ReportReader > switches report reading density without changing the report version | FAIL | 同上 | 是 |
| `src/components/report-reader.test.tsx` > ReportReader > cancels the historical loading state when returning to the current version | FAIL | 同上 | 是 |
| `src/components/report-reader.test.tsx` > ReportReader > requires inline confirmation before rollback and reloads backend state | FAIL | 同上 | 是 |
| `src/components/report-reader.test.tsx` > ReportReader > downloads the selected version and reports PDF failures locally | FAIL | 同上 | 是 |
| `src/components/report-presentation.test.tsx` > ReportPresentation | FAIL | 找不到「当前为快速版阅读」文案（test:39，快速版提示语与实现不同步） | 是 |

另有 1 条 vitest unhandled error（沙箱 node fs shim 对 Temp 目录写文件 EPERM），属运行环境噪声，与代码无关，不计入基线。

---

*基线建立：2026-09-24 19:54（Phase 0）。此后每 Phase 合入前重跑双端，对比本表。*
