# ----- Python -----
__pycache__/
*.pyc
*.pyo

# ----- 本地工具缓存 / 日志 -----
.reasonix/
reasonix.toml
*.log
rewrite_log_batch*.txt
lo_install.log
/.runtime/

# ----- 本次整治的临时反推脚本（非项目资产）-----
_rebuild_reports.py

# ----- 本地助手记忆（项目级，不入库）-----
.workbuddy/

# ----- 生成的报告产物（docx/pdf/png/html，可由引擎再生，不入库） -----
/reports/

# ----- backend 虚拟环境与运行时数据（源码入库，环境/产物不入库） -----
backend/.venv/
backend/venv/
backend/runtime/

# ----- 清理暂存区（重复/废弃报告的临时回收区，可恢复，不入库） -----
.trash_local/

# ----- 历史孤儿归档（旧 backend 适配器 / 旧 frontend 快照，仅本地留档，不入库） -----
_archived/

# ----- backend 运行时与密钥（源码入库，环境/产物/密钥不入库） -----
backend/.venv/
backend/.venv-broken/
backend/venv/
backend/runtime/
backend/generated/
backend/data/

# ----- 后端测试/调试临时区（pytest 以 cwd 为 tempdir，产物可再生，不入库） -----
backend/.runtime/

# ----- 内部计划 / 临时文档（仅本地留档，不公开） -----
DEPLOY_PLAN.md
GO_TO_MARKET_PLAN.md
UPGRADE_PLAN.md
LEVEL_AND_PACKAGING.md
PROJECT_ASSESSMENT.md
USER_SYSTEM_PLAN.md

# ----- 本地备份脚本与已废弃的旧备份方案（含私有仓库路径，不进公开仓库） -----
备份到GitHub.bat
_废弃_旧备份方案/

# ----- API 推送兜底工具（本机 git 直连 github 被 502 阻断时改走 REST API，本地工具，不入库） -----
.api_push*.py

# ----- 引擎调试预览页（本地调试产物，不入库） -----
/outputs/

# ----- 私有备份仓库（独立 git 目录，仅本地使用，绝不进公开仓库） -----
/.git-backup/
/.git-backup-ignore
_backup_setup.log

# ----- 项目内回收站（2026-09-07 清理，确认无误后可整目录删除） -----
/_trash_*/

# ----- 损坏仓库留档（2026-09-07 pack 文件丢失事故，确认无遗留数据后可删） -----
/.git_broken_*/

# SQLite 备份输出（backup_db.py）
backend/backups/
